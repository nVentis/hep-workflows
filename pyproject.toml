[project]
name = "hep_workflows"
dynamic = ["version"]
dependencies = [
    "law==0.1.20",
    "luigi==3.5.1",
    "numpy==1.26.4",
    "python-dateutil==2.8.2",
    "tqdm==4.66.3"
]

[tool.setuptools]
packages = ["hep_workflows"]

[tool.setuptools.package-data]
tests = ["*.py"]